FROM debian:12-slim

LABEL org.opencontainers.image.title="IR Malware Analysis Sandbox Base"
LABEL org.opencontainers.image.description="Analysis base image for the SecurityOps shell workflow."

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
  && apt-get install -y --no-install-recommends \
    ca-certificates \
    coreutils \
    procps \
    psmisc \
    lsof \
    strace \
    tcpdump \
    iproute2 \
    iputils-ping \
    dnsutils \
    file \
    less \
    bash \
  && rm -rf /var/lib/apt/lists/*

RUN useradd --create-home --uid 1000 --shell /bin/bash forensics

WORKDIR /analysis
USER forensics
ENTRYPOINT ["/bin/bash"]
CMD ["-l"]
