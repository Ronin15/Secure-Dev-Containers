# Default allowlist for controlled egress profile.
# Entries are hostnames, one per line.
#
# Keep this file empty to deny all outbound egress by default.
# Add explicit hostnames only after review.
# e.g.
# example.com
